streamlit>=1.28.0,<2.0
pandas>=2.1.0,<3.0
plotly>=5.17.0,<6.0
openpyxl>=3.1.0,<4.0

# Monthly automation pipeline (Jobs A + B)
# QuickBooks Online Reports API
python-quickbooks>=0.9.0,<1.0
intuit-oauth>=1.2.4,<2.0
# FileMaker Cloud Data API — lightweight native client; swap to python-fmrest if preferred later
requests>=2.31.0,<3.0
# Secure credential storage (Windows Credential Manager on our target host)
keyring>=24.0.0,<26.0
